# Vercel → Project Settings → Environment Variables (Production + Preview)

# Absolute BE API base (no trailing slash issues — include /api/v1)
# Example: https://doctor-ri-api.up.railway.app/api/v1
VITE_API_BASE_URL=https://YOUR-BE-HOST/api/v1
